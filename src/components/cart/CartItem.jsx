import {imageUrl,money} from "../../utils/helpers";import {useCart} from "../../context/CartContext";
export default function CartItem({item:x}){const {update,remove}=useCart();return <div className="cart-item"><img src={imageUrl(x.image_url)}/><div className="cart-item-main"><span className="muted">{x.brand}</span><h3>{x.name}</h3><span>{money(x.discount_price??x.price)} each</span></div><div className="qty"><button onClick={()=>update(x.id,x.quantity-1)}>−</button><b>{x.quantity}</b><button onClick={()=>update(x.id,x.quantity+1)}>+</button></div><b className="line-price">{money((x.discount_price??x.price)*x.quantity)}</b><button className="remove" onClick={()=>remove(x.id)}>×</button></div>}
